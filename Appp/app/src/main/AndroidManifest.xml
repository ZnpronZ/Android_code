<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/music1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appp">
        <activity android:name=".login.LoginCreate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".login.LoginMain" />
        <activity android:name=".mainmusic.Activity_bai_hat" />
        <activity android:name=".mainmusic.MainMusicActivity" >

        </activity>
        <activity android:name=".ui.Home">


        </activity>
        <activity android:name=".ui.Video" />
        <activity android:name=".ui.MovieDetailActivity" />
        <activity android:name=".ui.MainActivity" />



        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>